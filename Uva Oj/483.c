#include<stdio.h>
#include<string.h>
int main()
{
    int lenth, n, i, j, look = 1;
    char str[1000], rev[1000];

    while(gets(str)){
        for(i = 0;  str[i] != '\o';i++)
        if((str[i] >= 'a' && str[i] <= 'z') || (str[i] >= 'A' && str[i] <= 'Z')){
            if(look == 1)
        }
    }
    return 0;
}
