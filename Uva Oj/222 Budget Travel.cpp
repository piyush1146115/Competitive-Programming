#include<bits/stdc++.h>
using namespace std;

double target, cap, mpg, ini;
int n;

int main()
{
  while(scanf("%lf", &target) == 1){
    if(target < 0)
        break;
    scanf("%lf %lf %lf", &cap, &mpg, &ini);
    scanf("%d", &n);
    for(int i = 0; i < n; i++){

    }
  }
}
